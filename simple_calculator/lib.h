#ifndef LIB_H
#define LIB_H

#include <stdio.h>
#include <stdlib.h>

/** function prototypes **/

float add(float, float);
float sub(float, float);
float mult(float, float);
float divi(float, float);
float operatr(char, float, float);

#endif
